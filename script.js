document.getElementById("calculateBtn").addEventListener("click",calculateMortgage);function money(v){return v.toLocaleString(undefined,{style:"currency",currency:"USD",minimumFractionDigits:2,maximumFractionDigits:2})}function calculateMortgage(){const p=parseFloat(price.value),d=parseFloat(down.value),r=parseFloat(rate.value),y=parseInt(term.value,10),m=document.getElementById("message"),res=document.getElementById("results");if(!Number.isFinite(p)||p<=0||!Number.isFinite(d)||d<0||d>=p||!Number.isFinite(r)||r<0||!Number.isFinite(y)||y<=0){m.textContent="Please enter valid mortgage information.";res.hidden=true;return}const principal=p-d,n=y*12,mr=r/100/12;let pay=mr===0?principal/n:principal*mr/(1-Math.pow(1+mr,-n));const totalPaid=pay*n,totalInterest=totalPaid-principal;monthly.textContent=money(pay);loan.textContent=money(principal);interest.textContent=money(totalInterest);total.textContent=money(totalPaid);m.textContent="";res.hidden=false}